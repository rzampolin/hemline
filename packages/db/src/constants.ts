/**
 * Shared db-level constants.
 *
 * DEMO_USER_ID lives here (not in seed.ts) so runtime code and tests can
 * import it without side effects — seed.ts self-executes when run as a script.
 */
export const DEMO_USER_ID = '00000000-0000-4000-8000-000000000001';
