/**
 * Long-running scheduler — `npm run ingest:watch`.
 * TODO(data-eng): read `sources` rows, register node-cron jobs per
 * cadence_cron, run pipeline.ts per tick. docs/ARCHITECTURE.md §8.
 */
console.log(
  '[ingest:watch] not yet implemented (data-eng): node-cron scheduler — docs/ARCHITECTURE.md §8',
);
process.exit(0);
